/**
 * @theatre/core and @theatre/studio are installed per product spec.
 * Studio is for authoring; wire sequences here when you want timeline-driven motion.
 * No runtime import yet — avoids bundling studio in production unless code-split.
 */

export {};
